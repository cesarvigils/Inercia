// menu.js (project root)
//
// This file is empty. It does not appear to be referenced by any current HTML page — every page that
// needs the side-menu script loads js/menu.js instead (see index.html, contacto.html, reservas.html,
// standings.html). Looks like a leftover/orphaned file from before the js/ folder existed; safe to
// investigate for removal, but left untouched here since removing files is a code change, not a comment.
